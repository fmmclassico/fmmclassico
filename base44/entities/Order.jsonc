{
  "name": "Order",
  "type": "object",
  "properties": {
    "order_number": {
      "type": "string",
      "description": "Unique order number"
    },
    "items": {
      "type": "array",
      "description": "Order items",
      "items": {
        "type": "object",
        "properties": {
          "product_id": { "type": "string" },
          "product_name": { "type": "string" },
          "product_image": { "type": "string" },
          "price": { "type": "number" },
          "quantity": { "type": "number" }
        }
      }
    },
    "total_amount": {
      "type": "number",
      "description": "Total order amount charged to Hubtel"
    },
    "delivery_fee": {
      "type": "number",
      "description": "Delivery fee charged"
    },
    "balance_due": {
      "type": "number",
      "description": "Amount to be paid on delivery (0 for full payment)"
    },
    "payment_method": {
      "type": "string",
      "enum": ["full_payment", "deposit_balance", "pay_on_delivery"],
      "description": "Payment method chosen at checkout"
    },
    "delivery_zone": {
      "type": "string",
      "description": "Delivery zone selected"
    },
    "payment_status": {
      "type": "string",
      "enum": ["pending_payment", "paid", "failed", "cancelled", "refunded"],
      "default": "pending_payment",
      "description": "Payment status. Initially pending_payment - update to 'paid' when payment is confirmed."
    },
    "status": {
      "type": "string",
      "enum": ["confirmed", "processing", "packed", "shipped", "out_for_delivery", "delivered", "cancelled", "returned"],
      "default": "confirmed",
      "description": "Fulfillment status. IMPORTANT: Do NOT change to 'processing' unless payment_status is 'paid'. Check BOTH payment_status AND status before fulfilling orders."
    },
    "customer_name": { "type": "string" },
    "customer_email": { "type": "string" },
    "customer_phone": { "type": "string" },
    "delivery_address": { "type": "string" },
    "city": { "type": "string" },
    "map_location": { "type": "string", "description": "GPS link" },
    "notes": { "type": "string" },
    "estimated_delivery": { "type": "string", "format": "date" },
    "tracking_updates": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "status": { "type": "string" },
          "message": { "type": "string" },
          "timestamp": { "type": "string" }
        }
      }
    },
    "hubtel_transaction_id": { "type": "string", "description": "(Deprecated)" },
    "hubtel_status": { "type": "string", "enum": ["initiated", "pending", "successful", "failed"], "default": "initiated", "description": "(Deprecated)" },
    "payment_reference": { "type": "string", "description": "(Deprecated)" }
  },
  "required": ["items", "total_amount", "customer_name", "customer_email", "delivery_address"],
  "rls": {
    "create": {
      "$or": [
        { "data.customer_email": "{{user.email}}" },
        { "user_condition": { "role": "admin" } }
      ]
    },
    "read": {
      "$or": [
        { "data.customer_email": "{{user.email}}" },
        { "user_condition": { "role": "admin" } }
      ]
    },
    "update": { "user_condition": { "role": "admin" } },
    "delete": { "user_condition": { "role": "admin" } }
  }
}
